import React from 'react';
import footer from '../Assets/footer.png'
const Footer = () => {
    return (
        <div className={'container'}>
            <img className={'w-100'} src={footer} alt="footer"/>
        </div>
    );
};

export default Footer;